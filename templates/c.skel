#include<stdio.h>
#include<stdlib.h>
#include<time.h>


void start () {
	time_t curTime;
	time(&curTime);
	printf("Start Here %s", ctime(&curTime));
}

int main(int argc, char *argv[] ){
	start();	
}
