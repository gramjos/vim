*vim9_dev.txt* Vim9script, Python, and Tooling.

DEV REFERENCE & VIM9SCRIPT                     *vim9-dev*

This supports the |job-hunt| and |obsidian-project|.

==============================================================================
VIM9SCRIPT SYNTAX                              *vim9-syntax*

String Interpolation (The "M4" way):
    var path = $"/home/{user}/docs"
    # remember: use .. for concat, not +

Async Jobs (for Gemini Plugin):
    var job = job_start(cmd, {
        out_cb: MyCallback,
        err_cb: MyError
    })

==============================================================================
PYTHON SNIPPETS (DATA SCIENCE)                 *python-snippets*

For |vqa-project| and parsing:

    import json
    import yaml # PyYAML for frontmatter

==============================================================================
MACOS M4 TERMINAL                              *mac-setup*

- Ensure "Option as Meta" is ON in Terminal.app settings.
- Drawing: iPad + Pencil used for Excalidraw/Whiteboarding.

 vim:tw=78:ts=8:noet:ft=help:norl:
