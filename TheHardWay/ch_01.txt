Vim Run Commands (vimrc)

Code in the .vimrc will run every time vim is started

from within vim :echo $MYVIMRC to display path to .vimrc

echo command will print to the vim window

echom will print to the message-history which can be accessed 
	by :messages 
		:mes

MapLocalLeader
	a leader name space for specific buffer types


